{
  "name": "chipfolio",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r build/* ../",
    "start": "cd frontend && npm start"
  },
  "engines": {
    "node": ">=16.x"
  }
} 